# The project, Agent Runtime region, and numeric reasoning-engine ID.
# Find the ID in Agent Platform -> Deployments -> your runtime.
PROJECT_ID=your-gcp-project-id
LOCATION_ID=us-central1
RESOURCE_ID=your-agent-runtime-id

# Optional. The script creates a new session unless --session-id is supplied.
USER_ID=gateway-test-user
